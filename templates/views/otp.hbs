<!DOCTYPE html>
<html lang="en">
{{!-- <head>
  
</head> --}}
<body >
{{>navbar}}

    {{>headerlinks}}
    <form action="/otp" method="POST" class="formData"  id="forgetPassDiv">
    <div class="container">
        <div class="form-floating">
  <input name="username"
            id="username"
            value=""
            type="username"
            placeholder="Username"
            class="form-control"
            required >
  <label for="userLoginId">Username</label>
</div>
        <div class="form-floating">
  <input name="otp"
            id="otp"
            value=""
            type="number"
            placeholder="Enter OTP"
            class="form-control"
            required >
  <label for="email">OTP</label>
</div> 
        <button type="submit" class="btn btn-primary btn-reset">Submit</button>
</form>
 {{#if message}}
     <div class="mt-4">
      <h4 class="alert alert-denger">{{message}}</h4>
      </div>
      {{/if}}

</body>
</html>